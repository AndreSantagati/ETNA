# Core dependencies
requests>=2.25.1
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
seaborn>=0.11.0
pyyaml>=5.4.0

# Sigma rule support (more stable versions)
pysigma>=0.9.0
pysigma-backend-splunk>=1.0.0

# Optional: Only install if available
# sigma-cli>=0.7.0